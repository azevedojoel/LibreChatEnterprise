const express = require('express');

/**
 * Artifacts API routes.
 * Reserved for future artifact-related endpoints (e.g. artifact storage, sharing).
 * The former POST /pdf endpoint was removed in favor of client-side print-to-PDF.
 */
const router = express.Router();

module.exports = router;
