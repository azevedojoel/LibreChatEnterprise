# Copy to .env.railway and fill in. Used by scripts/sync-railway-env.js
# NEVER commit .env.railway - it contains secrets
#
# Usage:
#   1. cp .env.railway.example .env.railway
#   2. Fill in real values (get MONGO_URI from Railway MongoDB plugin, etc.)
#   3. RAILWAY_TOKEN=xxx RAILWAY_PROJECT_ID=xxx RAILWAY_ENVIRONMENT_ID=xxx node scripts/sync-railway-env.js

# === Required for sync script (get from Railway dashboard, Cmd+K) ===
RAILWAY_PROJECT_ID=your-project-id
RAILWAY_ENVIRONMENT_ID=your-environment-id
RAILWAY_SERVICE_ID=your-librechat-service-id

# === Railway API token (https://railway.com/account/tokens) ===
RAILWAY_TOKEN=your-account-or-project-token

# === LibreChat - use Railway variable references when possible ===
# MONGO_URI - use ${{MongoDB.MONGO_URL}} if you have Railway MongoDB plugin
MONGO_URI=mongodb+srv://...

# DOMAIN - your production URL
DOMAIN_CLIENT=https://your-app.up.railway.app
DOMAIN_SERVER=https://your-app.up.railway.app

# === Proxy (required for cookies/auth behind Railway) ===
TRUST_PROXY=1

# === Secrets (generate secure values) ===
CREDS_KEY=
CREDS_IV=
JWT_SECRET=
JWT_REFRESH_SECRET=
MEILI_MASTER_KEY=

# === API keys for providers you use ===
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_KEY=

# === Code execution (use local for in-process Python, no API key needed) ===
# LIBRECHAT_CODE_API_KEY=local

# === Google Workspace MCP (optional - only if using google-workspace MCP) ===
# GOOGLE_WORKSPACE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_WORKSPACE_CLIENT_SECRET=your-client-secret

# === Microsoft 365 MCP (optional - only if using ms-365 MCP) ===
# MS365_MCP_CLIENT_ID=your-azure-app-client-id
# MS365_MCP_CLIENT_SECRET=your-client-secret

# === Production tweaks ===
NODE_ENV=production
DEBUG_LOGGING=false
NO_INDEX=false
